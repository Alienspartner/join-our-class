<view class="page">
    <view class="weui-form">
      <view class="weui-form__text-area">
        <view class="weui-form__title">{{titleOne}}</view>
        <view class="weui-form__desc">{{titleTwo}}</view>
      </view>
      <view class="weui-btn-area">
                <button class="weui-btn" type="default" bindtap="{{buttonFunc}}">{{buttonText}}</button>
            </view>
     </view>
</view>
<wux-keyboard id="wux-keyboard" />
